# =============================================================================
# LOCAL DEVELOPMENT ENVIRONMENT VARIABLES (NEVER commit to Git)
# =============================================================================
# This file contains sensitive credentials for local development only.
# It overrides values from .env and is automatically ignored by Git.
# =============================================================================

# Database - Supabase PostgreSQL Connection
# IMPORTANT: Use the SESSION POOLER URL from Supabase (not Direct or Transaction pooler)
# Get this from: Supabase Dashboard → Project Settings → Database → Connection String → Session pooler
# Example format: postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-us-east-1.pooler.supabase.com:5432/postgres
DATABASE_URL=postgresql://postgres.argcegbintpiruogjgic:ayphenTextile@aws-1-us-east-1.pooler.supabase.com:5432/postgres

# Note: DIRECT_URL is removed - using pooled connection only for better performance

# Redis Connection (if using Redis locally)
# REDIS_URL=redis://localhost:6379
# REDIS_PASSWORD=  # Uncomment if your local Redis requires a password

# JWT Secrets - GENERATED FOR LOCAL DEVELOPMENT
# These are cryptographically secure secrets for local testing
# For production, use the secrets you added to Render
JWT_SECRET=c0bf8c7bfffa60edc2a79efa64daef865a34b4082591abfaffa8f603d73a7bc4
JWT_REFRESH_SECRET=53f41a04b5ddb3301250bd550698409295dada23d32e7ff49ca48349a925f9f9
SESSION_SECRET=8ebb8e288d0a0f85f23dc6d2abc3ec78855165bfc9a34185a70717ba743c99b4

# Google OAuth (Get from Google Cloud Console)
# https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:5001/api/v1/auth/google/callback

# Email Configuration (for local testing)
# If you want to test email functionality locally, add your SMTP credentials here
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-specific-password

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3002

# =============================================================================
# NOTES
# =============================================================================
# 1. This file is gitignored and should NEVER be committed
# 2. Copy this file to .env.local.example (without sensitive values) if you
#    want to provide a template for other developers
# 3. For production, set these variables in your hosting platform (Render)
# 4. The JWT secrets above are for LOCAL DEVELOPMENT ONLY
#    Use different secrets for production (already added to Render)
# =============================================================================
