# =============================================================================
# SHARED ENVIRONMENT VARIABLES (Safe Defaults - CAN be committed to Git)
# =============================================================================
# This file contains non-sensitive defaults and documentation.
# For local development, override these in .env.local (which is gitignored)
# For production, set these as environment variables in Render/hosting platform
# =============================================================================

# Environment
NODE_ENV=development

# Server Configuration
PORT=5001
HOST=0.0.0.0

# API Configuration
API_VERSION=v1
API_PREFIX=/api

# Database Configuration (Local Development Defaults)
# IMPORTANT: Override in .env.local with your actual database credentials
DB_HOST=localhost
DB_PORT=5432
DB_NAME=lavoro_ai_ferri
DB_USER=postgres
DB_PASSWORD=
DB_SSL=false
DB_MAX_CONNECTIONS=20
DB_IDLE_TIMEOUT=30000
DB_CONNECTION_TIMEOUT=5000

# Redis Configuration (Local Development Defaults)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
# REDIS_PASSWORD=  # Uncomment and set in .env.local if Redis requires auth

# JWT Configuration (Token Expiration Times)
JWT_EXPIRES_IN=3d
JWT_REFRESH_EXPIRES_IN=7d

# CORS Configuration
CORS_ORIGIN=http://localhost:3002
CORS_CREDENTIALS=true

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info
LOG_FILE=logs/app.log

# Security Configuration
BCRYPT_ROUNDS=10

# File Upload Configuration
MAX_FILE_SIZE=5242880
UPLOAD_PATH=uploads/

# Multi-Tenancy
DEFAULT_TENANT_SCHEMA=public
TENANT_SCHEMA_PREFIX=tenant_

# Email Configuration (Development Mode)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_FROM=noreply@lavoro-ai-ferri.com
# EMAIL_USER=  # Set in .env.local
# EMAIL_PASSWORD=  # Set in .env.local

# =============================================================================
# SENSITIVE VARIABLES - DO NOT SET HERE
# =============================================================================
# The following variables contain sensitive data and MUST be set in .env.local
# (for local development) or in your hosting platform's environment variables
# (for production). NEVER commit actual values to Git.
#
# Required in .env.local:
# - DATABASE_URL (or individual DB_* credentials)
# - JWT_SECRET
# - JWT_REFRESH_SECRET
# - SESSION_SECRET
# - GOOGLE_CLIENT_ID
# - GOOGLE_CLIENT_SECRET
# - EMAIL_USER
# - EMAIL_PASSWORD
# =============================================================================
