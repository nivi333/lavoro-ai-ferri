# Netlify Configuration for Ayphen Textile Frontend

[build]
  # Build command
  command = "npm ci --legacy-peer-deps && npm run build"
  
  # Directory with build output
  publish = "dist"
  
  # Node version
  [build.environment]
    NODE_VERSION = "18"

# Redirect rules for SPA routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Headers for security and CORS
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"

# Cache static assets
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Production environment variables (set in Netlify UI)
# VITE_API_BASE_URL = https://ayphentextilebackend-7ic9arjz.b4a.run/api/v1
# VITE_APP_NAME = Ayphen Textile
# VITE_APP_VERSION = 1.0.0
